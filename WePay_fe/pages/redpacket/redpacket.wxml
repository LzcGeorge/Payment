<view class="container">

  <view class="title">当前余额：￥{{ balance }}</view>
  <button bindtap="onSignIn" loading="{{loading}}" type="primary">红包签到</button>
  <button bindtap="onConfirmTransfer" disabled="{{!package_info}}">确认转账</button>
 
</view>